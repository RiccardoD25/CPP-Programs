// petApp.cpp : This file contains the 'main' function. Program execution begins and ends there.
// COP1334C intro to C++
// Riccardo De Simini

#include "pet.h"

int main()
{
	int name; // Local variable for name
	string m = type;   // Local variable for type
	int age = 0;     // Local variable for age

	cout << "Enter model year: ";
	cin >> name;

	cout << "Enter make: ";
	cin >> type;

	// Define a instance of the pet class
	Pet Pet PET_H = (name, type),

	// Show values for the pet
	cout << "\n\nThe pet name is " << Pet.getName() << endl;
	cout << "The pet type is " << Pet.getType() << endl;

	// Show pet age
	for (int i = 1; i <= 5; i++)
	{
		Pet.gender();
	}
	cout << "The pet age is " << Pet.getAge() << endl;

	return 0;
}

